# Path Following Guide

`MovementPath` is an ordered list of caller-supplied waypoints. World Navigation follows those points in order and reports progress.

The package does not build paths and does not include A*, NavMesh, grids, lane construction, obstacle avoidance, steering, or flocking.
